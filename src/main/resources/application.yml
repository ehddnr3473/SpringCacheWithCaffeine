spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/testdb
    username: root
    password: 3473

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cache:
    type: caffeine

server:
  port: 8080